# Smart Dashboard - Environment Variables Template
# Copy this file to .env.local for development or .env.production for production

# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Get these from your Supabase project settings

# Public Supabase URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Public anonymous key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key (KEEP SECRET - server-side only)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Supabase URL (server-side duplicate for consistency)
SUPABASE_URL=your_supabase_project_url

# Supabase anonymous key (server-side duplicate)
SUPABASE_ANON_KEY=your_supabase_anon_key

# =============================================================================
# Authentication Redirect URLs
# =============================================================================
# Set to your domain for production, localhost for development

# Development redirect URL
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000

# Production redirect URL (uncomment and set for production)
# NEXT_PUBLIC_SUPABASE_REDIRECT_URL=https://your-domain.com

# =============================================================================
# Application Configuration (OPTIONAL)
# =============================================================================

# Environment mode
NODE_ENV=development

# Disable Next.js telemetry (recommended)
NEXT_TELEMETRY_DISABLED=1

# Port configuration (default: 3000)
# PORT=3000

# =============================================================================
# Docker Development Settings (OPTIONAL)
# =============================================================================

# Enable file watching in Docker containers
WATCHPACK_POLLING=true
CHOKIDAR_USEPOLLING=true

# Browser setting for containerized development
BROWSER=none